import CertLayout from "@/components/certifications/cert-layout";

import { getPageMetadata } from "@/config/metadata";

export async function generateMetadata(props) {
  const params = await props.params;
  const { lang } = params;
  return getPageMetadata(lang, {
    title: certData.title,
    description: `Certification: ${certData.title} from ${certData.issuer}`,
    image: certData.image,
  });
}

export const certData = {
  title: "AWS Certified Solutions Architect – Professional",
  date: "2024-08-05",
  image: "/certifications/aws-solutions-architect-professional.png",
  issuer: "Amazon Web Services (AWS)",
  slug: "aws-solutions-architect-professional",
};

Le sommet de l'architecture AWS. Cette certification **Professional** valide ma capacité à résoudre des problèmes de conception complexes et à orchestrer des environnements multi-comptes.

<Callout type="success">
C'est le plus haut niveau de certification architecturale, prouvant une expertise stratégique au niveau enterprise.
</Callout>

### Maîtrise de l'Architecture
- **Complexité Organisationnelle** : Utilisation intensive d'AWS Organizations et SCPs.
- **Modernisation d'Applications** : Migration de monolithes vers des microservices serverless.
- **Réseau Avancé** : Transit Gateway, Direct Connect et architectures hybrides.
- **Gouvernance & Conformité** : Automatisation des audits avec AWS Config.

<TechBadge name="Enterprise Architecture" /> <TechBadge name="AWS Professional" />


export default async function Layout(props) {
  const params = await props.params;
  return <CertLayout cert={certData} lang={params.lang}>{props.children}</CertLayout>;
}
