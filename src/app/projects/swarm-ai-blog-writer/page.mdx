import { getCurrentLanguage } from "@/lib/dictionary";
import ProjectPostLayout from "@/components/projects/project-post-layout";
import { getPageMetadata } from "@/config/metadata";

export async function generateMetadata() {
  const lang = await getCurrentLanguage();
  return getPageMetadata(lang, {
    title: projectData.title,
    description: projectData.description,
    image: projectData.image,
  });
}

export const projectData = {
  title: "Swarm AI Blog Writer",
  description: "Production-grade multi-agent blog generation engine powered by Pydantic AI and Groq (Llama 3.3 70B).",
  dates: "March 2026",
  active: true,
  order: 1,
  status: "completed",
  category: "opensource",
  image: "/portfolio/sleek-ai/th.png",
  tags: ["Vue.js 3", "TypeScript", "Vite 5", "Flask", "Python", "Pydantic v2", "Groq API"],
  links: [{ type: "github", href: "https://github.com/Arfazrll/Swarm-Agent-Orchestrator" }],
  images: ["/portfolio/sleek-ai/th.png"],
}

# <L en="Swarm AI Orchestration" fr="Orchestration Swarm AI" />
<L 
  en="A production-grade multi-agent engine designed to generate deep, research-backed technical content using Llama 3.3 70B." 
  fr="Un moteur multi-agents de niveau production conçu pour générer du contenu technique approfondi et documenté utilisant Llama 3.3 70B."
/>

![Swarm AI Hero](/portfolio/sleek-ai/th.png)

## <L en="The Vision: Reliable Multi-Agent Pipes" fr="La Vision : Pipelines Multi-Agents Fiables" />
<L 
  en="The goal was to create a research assistant that doesn't just write, but plans and verifies. We leveraged Pydantic v2 for strict schema enforcement." 
  fr="L'objectif était de créer un assistant de recherche qui ne se contente pas d'écrire, mais qui planifie et vérifie. Nous avons utilisé Pydantic v2 pour une application stricte des schémas."
/>

### <L en="Multi-Agent Orchestration" fr="Orchestration Multi-Agents" />
- **Planner Agent**: <L en="Generates a structured outline with Pydantic-validated schema." fr="Génère un plan structuré avec un schéma validé par Pydantic." />
- **Researcher Agent**: <L en="Performs deep-dive research for each section." fr="Effectue des recherches approfondies pour chaque section." />
- **Writer Agent**: <L en="Crafts a 1000+ word final article based on research." fr="Rédige l'article final de plus de 1000 mots à partir des recherches." />

### <L en="Industrial SaaS UI" fr="Interface SaaS Industrielle" />
<L 
  en="The dashboard features a high-performance bento-grid layout with real-time orchestration tracking via GSAP." 
  fr="Le tableau de bord présente une disposition bento-grid haute performance avec un suivi de l'orchestration en temps réel via GSAP."
/>

- **Framework**: <TechBadge name="Vue" /> 3 + <TechBadge name="Flask" />
- **LLM Engine**: <TechBadge name="Groq" /> (Llama 3.3 70B)
- **Validation**: <TechBadge name="Python" /> Pydantic v2

export default async function Layout(props) {
  const lang = await getCurrentLanguage();
  return <ProjectPostLayout project={projectData} lang={lang}>{props.children}</ProjectPostLayout>;
}
