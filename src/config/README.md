# Application Configuration (`config/`)

System-level settings, SEO protocols, and global constants.

| File | Primary Export | Role |
|---|---|---|
| `metadata.ts` | `getPageMetadata()` | Orchestrates dynamic OpenGraph, Twitter, and JSON-LD tags |
| `opengraph/` | Directory | Assets and logic for dynamic social image generation |

> Configurations here are mostly **Server-Side** to ensure optimal performance and SEO indexing.
